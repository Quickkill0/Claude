{
  "mcpServers": {
    "permissions": {
      "command": "node",
      "args": ["{{APP_DIR}}/dist/main/mcp-server.js"],
      "env": {
        "PERMISSIONS_DIR": "{{PERMISSIONS_DIR}}"
      }
    }
  }
}